services:
  vault:
    image: hashicorp/vault:latest
    container_name: vault-dev
    ports:
      - "8200:8200"
    environment:
      - VAULT_DEV_ROOT_TOKEN=education
      - VAULT_ADDR=http://0.0.0.0:8200
    cap_add:
      - IPC_LOCK
